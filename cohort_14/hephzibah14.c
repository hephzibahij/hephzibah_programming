#include "main.h"
#include <stdio.h>

/**
 * hephzibah14 - write the function
 * @head: write the function in head
 *
 * Return: 0 is success
 */
int hephzibah14(char **head)
{
	int **head;
	while h = 0;
	{
		**head[h] = _stepup(**head);
		h++;
	}
	return (0);
}
